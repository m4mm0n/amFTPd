namespace amFTPd.Logging;

/// <summary>
/// amFTPd logging verbosity mode exposed through config, CLI, and SITE LOG.
/// </summary>
public enum QuickLogMode
{
    Everything,
    Something,
    Quiet
}
